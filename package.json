{
	"type": "module",
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": ">=24.0.0",
			"onFail": "download"
		}
	},
	"scripts": {
		"check": "tsc --build",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"test": "vitest run"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"@fission-ai/openspec": "^1.2.0",
		"typescript": "^6.0.0",
		"vite": "^8.0.0",
		"vitest": "^4.0.0"
	}
}
