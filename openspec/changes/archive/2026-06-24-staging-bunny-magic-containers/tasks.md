> **Status (agent):** the HCL for §1–§2 is authored in `infrastructure/bunny-staging.tf` (+ provider pin in `main.tf`, `var.bunnynet_api_key` in `variables.tf`, multi-platform lockfile) and is `tofu validate`-clean (provider 0.15.1; needed `version = 2` on the resource and no `protocols` on the CDN port). Remaining for these tasks is operator-side: add the `BUNNYNET_API_KEY` GHA secret, `tofu apply`, and the thin-apply discoveries (§1.4/§1.5). §3 (cache gate), §4 (DNS cutover), §5 (deploy rollout) are held until the §1.5/§4.1 discoveries land. §7 is human smoke.

## 1. Provider & thin-apply verification (do first)

- [x] 1.1 Add the `bunnynet` provider to `infrastructure/` — `required_providers { bunnynet = { source = "BunnyWay/bunnynet", version = "~> 0.15" } }`, provider block keyed on `var.bunnynet_api_key` (or `BUNNYNET_API_KEY` env). Add `BUNNYNET_API_KEY` to the GHA `apply-infra`/`plan-infra` secret set; `tofu init`; commit the lockfile delta.
- [x] 1.2 Resolve the public image registry via a data source (no creds): `data "bunnynet_compute_container_imageregistry" { registry = "GitHub", username = "" }`; use its `.id` for `container.image_registry`. Split the image into `image_namespace`/`image_name` (no domain) + `image_tag = "main"`.
- [x] 1.3 Declare a minimal `bunnynet_compute_container_app` for staging (`autoscaling_min=max=1`, `regions_required=["DE"]` (Frankfurt), container port 8080, `image_pull_policy="Always"`, one `volume {name="data" size=…}` + `volumemount {name="data" path="/data"}`, `PERSISTENCE_PATH=/data`). Keep `container`/`endpoint`/`env` blocks **alphabetized by name** (list-typed → unsorted = perpetual diff). **Thin-apply** and confirm the schema covers app + volume + endpoint + env + probe; record any gaps needing API/click-ops. — DONE: `tofu plan` clean (`1 to add, 0 change, 0 destroy`); needed `version = 2` + no CDN `protocols`. Registry data source resolved (`image_registry = 1156`). Secrets render `(sensitive value)`. No VPS/DNS collateral. OPEN: `image_digest = (known after apply)` → re-check drift after apply + a deploy cycle (§1.5); fallback `ignore_changes=[container[0].image_digest]`.
- [x] 1.4a `tofu apply` succeeded — app created + managed in state (id `lCmaflR0cPoLDbf`, region DE, one replica). TWO live gotchas hit + fixed: (a) **create initially 403'd** — account-side key-scope/billing, NOT our HCL (the API accepted the structure right up to auth; resolved by the operator on the Bunny side); (b) **provider bug** — leaving `regions_max_allowed` null makes the API default it to 5, failing tofu's post-apply consistency check. Fix: set `regions_max_allowed = 1` explicitly (now in `bunny-staging.tf`). Confirmed: `image_digest` IS stored in state (`sha256:…`) → §1.5 drift risk is live, watch after first `:main` push.
- [x] 1.4b App runs + healthy on `mc-p5hgd353u8.b-cdn.net`: `/readyz` = 200, `status:pass`, eventstore/domain/webhooks all pass, `gitSha=3010f8e4` (matches main). DuckDB initialized fine (eventstore check 2ms) → **deferred memory/OOM did NOT bite at boot/idle** (load-time headroom still TBD via the mem probe §7.3). Core viability confirmed: unmodified amd64 image + CDN HTTPS works.
- [x] 1.5 SUPERSEDED (Bunny docs: image update is the only re-pull trigger → adopted the digest-PATCH + `ignore_changes` path in §5/D5, not a tag-stable rollout). **Confirm the tag-stable rollout assumption:** trigger a rollout (`POST /mc/apps/{id}/deploy` or `.../restart`) with the unchanged `:main` tag + `Always` and verify it re-pulls a freshly-pushed digest. If it does NOT, switch the resource to `ignore_changes = [container[0].image_tag, container[0].image_digest]` and let the rollout own the image (see §5).

## 2. App configuration

- [x] 2.1 Add the CDN-type endpoint (NOT Anycast) → container port 8080, with `cdn { origin_ssl = false }` (edge→container plaintext, like Caddy→app today). Confirm automatic HTTPS on the `*.b-cdn.net` host. Pick a stable endpoint `name` (renaming a CDN endpoint can recreate the pull zone).
- [x] 2.2 Add the `readiness_probe` (`http`, path `/readyz`, container port).
- [x] 2.3 Declare every secret-bearing `TF_VAR_*` input as `sensitive = true` (so the unredacted-`env.value` does not leak into the `plan-infra` step summary). Render the `env` block from `TF_VAR_*` staging values (`GITHUB_OAUTH_CLIENT_ID`, `GITHUB_OAUTH_CLIENT_SECRET`, `AUTH_ALLOW`, `BASE_URL`, `PORT`, `PERSISTENCE_PATH`, workflow-secrets sealing key). Verify no secret value lands in any committed `*.tfvars`, and confirm a `plan` renders them as `(sensitive value)`.

## 3. CDN cache gating observation (do before any cutover)

- [x] 3.1 Dynamic route (`/readyz`, header-less 200) on `mc-p5hgd353u8.b-cdn.net` → `cdn-cache: MISS` on every request (Bunny even adds `cache-control: no-cache` downstream). Bunny does NOT cache header-less dynamic responses by default. ✅ safe.
- [x] 3.2 `/static/workflow-engine.css` → `cache-control: public, max-age=31536000, immutable`, `cdn-cache: MISS` then `HIT`. Static IS edge-cached; confirmed the desired split (static cached, dynamic not). ✅
- [x] 3.3 N/A — dynamic routes are NOT cached on Bunny defaults, so no edge rule / no Anycast fallback needed. Gating cache observation (cross-owner leak class) PASSES on defaults. App sets `cache-control` only on `/static/*` (verified in code); `no-cache` on `/readyz` is added by Bunny, not the app.

## 4. Cutover (single DNS record — mind the TLS ordering)

- [x] 4.1 Hostname attached via `bunnynet_pullzone_hostname.staging` wired to `...container[0].endpoint[0].cdn[0].pullzone_id` (resource attribute — NO `data bunnynet_pullzone` needed, §4.1 resolved). **TLS chicken-and-egg confirmed live:** `tls_enabled=true` at create → `loadFreeCertificate failed: domain is not pointing to our servers`. **Solution = TWO-PHASE:** phase 1 create with `tls_enabled=false`/`force_ssl=false` (registers hostname, no cert) + flip DNS; phase 2 (after DNS live) set both `true` → cert issues (11s). A failed cert-create leaves NO orphan hostname (rolls back).
- [x] 4.2 DNS cutover done — but Dynu **rejects in-place A→CNAME** (`501 "Record type change is not allowed"`). So NOT one in-place change: split into prod-only A-record resource + separate `restapi_object.dns_staging_cname` (CNAME, Dynu field = `host`). A+CNAME can't coexist → applied as TWO steps: `tofu apply -target=restapi_object.dns_a_record` (destroy staging A) then `tofu apply` (create CNAME). No VPS/prod collateral.
- [x] 4.3 `dig staging.workflow-engine.webredirect.org` → CNAME `mc-p5hgd353u8.b-cdn.net` → 185.111.111.158 (Bunny edge). Phase-2 cert issued. **Final verify: `curl https://staging.workflow-engine.webredirect.org/readyz` = `http=200 cert_verify=0`** (valid LE cert + app serving). CUTOVER COMPLETE. VPS staging stack untouched, still live as warm fallback.

## 5. Deploy pipeline (no TF image drift)

- [x] 5.1 `deploy-staging.yml` rewritten: build/push `:main` (capture `digest` output) → resolve app id by name → roll via `BunnyWay/actions/container-update-image@671d620…` (SHA-pinned = 0.2.2; receives the key so SHA-pinned not `@main`) with `image_tag:main`+`image_digest:<digest>` (action source verified to send both) → poll `/readyz` for `gitSha`. Bunny docs confirm **image update is the ONLY rolling-update trigger** (`/deploy`/`/restart` do NOT re-pull), so a changing digest is required. TF app resource has `lifecycle.ignore_changes = [container[0].image_tag, image_digest]` so CI-set digest doesn't fight `tofu apply` / break `plan-infra`. No `tofu` in the step. (Considered the bunny CLI — wrong model: project-deploy + app-linking, would fight TF. Inline curl also works; chose the SHA-pinned official action.) Validates green; needs `BUNNYNET_API_KEY` GHA secret.
- [ ] 5.2 Verify on first push to `main`: the Bunny app rolls forward and `/readyz` reports the new `gitSha`; AND a subsequent `plan-infra` is empty for the image fields (ignore_changes holds). This is the live test of the digest-PATCH rollout + the §1.5 drift answer.

## 6. Docs

- [x] 6.1 Document the staging Magic Containers deployment in `docs/infrastructure.md`: bunnynet resources, CDN endpoint, accept-loss volume durability (no backups/replication, reattach not guaranteed), the warm VPS fallback, and the one-record switch-back procedure (no toggle variable).
- [x] 6.2 Note the deferred DuckDB `memory_limit` decision and the observe-only posture (link the memory probe results from §7).

## 7. Cluster smoke (human)

> Edge/auth/infra surface — verified by a human against the live Bunny-served staging host, per CLAUDE.md.

- [ ] 7.1 **CDN cache leak check (gating):** from two browsers/sessions, `curl -D-` an authenticated owner-scoped route on `https://staging.workflow-engine.webredirect.org`; confirm no `cdn-cache: HIT` on dynamic routes and no cross-session content bleed.
- [x] 7.2 **OAuth round-trip:** DONE — GitHub login on the Bunny-served staging host works (session + owner-scoped page confirmed). **OAuth round-trip:** complete a GitHub login on the Bunny-served staging host; confirm the session cookie works and an owner-scoped page renders the right tenant's data.
- [ ] 7.3 **Memory/cpu visibility probe (observe only):** in the running container, capture `grep MemTotal /proc/meminfo`, `cat /sys/fs/cgroup/memory.max`, `nproc`, and DuckDB `SELECT current_setting('memory_limit')`. Record the values — this is the data that decides whether a future `memory_limit` fix is needed; no fix is applied now.
- [ ] 7.4 **Readiness + deploy rotation:** push a trivial commit to `main`; confirm the Bunny app rotates and `/readyz` reports the new `gitSha` within the deploy window.
- [ ] 7.5 **Switch-back rehearsal (optional):** re-target the staging record to the VPS IP, apply, confirm VPS-staging serves on the same hostname (Caddy re-issues cert), then re-target back to Bunny.
