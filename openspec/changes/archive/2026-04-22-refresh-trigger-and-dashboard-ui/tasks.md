## 1. Visual token foundation

- [x] 1.1 Introduce the type-scale tokens (`--fs-xs: 11px; --fs-sm: 12px; --fs-base: 13px; --fs-md: 14px; --fs-lg: 16px; --fs-micro: 10px`) in the `:root` block of `packages/runtime/src/ui/static/workflow-engine.css`; replace every hardcoded `font-size: Npx` and the `0.9em` usage with the matching token; drop the 9 px ruler label in favour of `--fs-micro` (10 px).
- [x] 1.2 Introduce the spacing scale (`--sp-1: 4px; --sp-2: 8px; --sp-3: 12px; --sp-4: 16px; --sp-5: 24px; --sp-6: 32px; --sp-7: 48px`); snap 14 → `--sp-3` (12), 10 → `--sp-2` (8), 20 → `--sp-5` (24); replace the odd `4px 16px 14px 16px` padding with a token-based rule and move the gap above the block it preceded.
- [x] 1.3 Consolidate radii into `--radius-sm: 4px; --radius: 8px; --radius-pill: 999px` (plus `50%` for dots); set `.badge` and `.filter-btn` to `--radius-pill`; set the scrollbar to `--radius-sm`; remove the 3/10/20 one-offs.
- [x] 1.4 Add the colour-role tokens `--on-accent: #fff`, `--overlay-strong: rgba(0,0,0,.45)`, `--overlay-weak: rgba(0,0,0,.08)`, `--shadow-modal: 0 10px 30px rgba(0,0,0,.18)`; redeclare `--overlay-weak: rgba(255,255,255,.06)` inside the `@media (prefers-color-scheme: dark)` block; replace every literal `#ffffff` used as foreground, `rgba(0,0,0,.08|.12|.4)` used as overlay, and the modal shadow literal with the tokens.
- [x] 1.5 Delete dead CSS fallbacks — specifically `var(--accent-primary, #0969da)` and `var(--accent-danger, #d1242f)` — by grepping for them and replacing with `var(--accent)` / `var(--red)` (or the correct existing token); the auth-card brand icon MUST now resolve through `--accent` in both modes.
- [x] 1.6 Fix the skeleton shimmer: replace the existing `bg-surface` gradient band in `.entry.skeleton::after` with `linear-gradient(90deg, transparent 0, color-mix(in srgb, var(--text) 6%, transparent) 50%, transparent 100%)`; verify in both modes by running `pnpm dev` and visually confirming the shimmer is faintly visible against `--bg-elevated` cards in both light and dark mode.
- [x] 1.7 Add the global `:focus-visible` ring: define `--focus-ring: 0 0 0 2px var(--bg-elevated), 0 0 0 4px var(--accent)` in `:root`; add the single rule `:where(a, button, select, input, textarea, summary, [role="button"]):focus-visible { outline: none; box-shadow: var(--focus-ring); border-radius: var(--radius-sm); }` and `:focus:not(:focus-visible) { outline: none; }`; tab through `/dashboard` and `/trigger` in the browser to confirm rings appear on every interactive element.
- [x] 1.8 Unify the icon set in `packages/runtime/src/ui/layout.ts` and both CSS files: replace the nav ASCII letters ("D", "T", "W") with inline-SVG lucide-style glyphs wrapped in `<span class="icon">`; replace the `▶` / `\25B6` details markers in `trigger.css` and the `›` chevron introduced later (task 3.8) with a single shared chevron SVG class `.icon-chevron`; add CSS `.icon { fill: currentColor; width: 16px; height: 16px }`; retain the `<select>` caret data-URI as the only background-image exception.

## 2. Shared trigger helpers

- [x] 2.1 Create `packages/runtime/src/ui/triggers.ts` exporting `triggerKindIcon(kind: string): HtmlEscapedString` (hono `<span class="trigger-kind-icon">…</span>` returning 🌐 for http, ⏰ for cron, fallback ● for unknown), `triggerKindLabel(kind: string): string` ("HTTP", "Cron", fallback `kind`), and `triggerCardMeta(descriptor: TriggerDescriptor): string` (`POST /webhooks/<tenant>/<workflow>/<name>` for http; `<schedule> (<tz>)` for cron).
- [x] 2.2 Delete the local `KIND_ICONS` and `kindIcon`/`renderKindIcon` helpers in `packages/runtime/src/ui/trigger/page.ts`; import from the shared module.
- [x] 2.3 Delete the local `KIND_ICONS` and `renderKindIcon` helper in `packages/runtime/src/ui/dashboard/page.ts`; import from the shared module — cron now renders ⏰ on the dashboard (fixing the previous fallback to ●).

## 3. Trigger page behaviour and layout

- [x] 3.1 In `packages/runtime/src/ui/trigger/page.ts`, bucket `entries` by `workflow.name`, sort groups and triggers alphabetically, and emit `<section class="trigger-group"><h2 class="trigger-group-title">{workflow}</h2>…cards…</section>` in `renderTriggerPage`; update the existing alpha-sort over the flat array to preserve intra-group alpha order.
- [x] 3.2 In `renderTriggerCard`, drop the `${workflow} / ` prefix on `.trigger-name` (the `workflow` is conveyed by the enclosing group).
- [x] 3.3 In `renderTriggerCard`, wrap the meta string in a `<span class="trigger-meta">`; style `.trigger-meta` in `trigger.css` with monospace type, muted colour, and `margin-left: auto` so it right-aligns inside `.trigger-summary`. For HTTP triggers only, also render an inline copy button next to the meta (reuse `.trigger-result-copy` styling from `workflow-engine.css`).
- [x] 3.4 In `renderTriggerCard`, detect whether the schema has no `properties` and no `additionalProperties`; when so, omit the `.form-container` div entirely and render only the Submit button. Keep the `<script type="application/json">` schema embed so client logic can still recognise the empty case.
- [x] 3.5 In `packages/runtime/src/ui/static/trigger-forms.js`, update `initForm` to early-return when the card has no `.form-container` (no Jedison instance created); in `submitTrigger`, treat a missing Jedison instance as an empty-object form value and still POST `{}`.
- [x] 3.6 In `trigger-forms.js` `submitTrigger`, on click: disable the Submit button, add `.submit-btn--loading` class, change label to `Submitting…`; in both `.then` and `.catch`, remove the class, restore the label, and re-enable the button just before calling `window.showResult` / `window.showResultBlocks`.
- [x] 3.7 In `trigger.css`, define `.submit-btn--loading` with a pure-CSS `@keyframes` spinner via a `::before` pseudo-element (1em inline-block rotating). Add a corresponding `@media (prefers-reduced-motion: reduce)` rule that suppresses the animation.
- [x] 3.8 In `trigger.css`, style `.trigger-group` (vertical spacing between groups via `--sp-5`) and `.trigger-group-title` (uses `--fs-md`, muted text, bottom-aligned with the cards).

## 4. Result dialog three-state outcome

- [x] 4.1 In `packages/runtime/src/ui/static/result-dialog.js`, refactor `showResultBlocks(blocks, ok)` to `showResultBlocks(blocks, status, body)` where `status` is the numeric HTTP status (or `null` for a network rejection) and `body` is the parsed response body (or `{error: string}` for a network rejection). Select the visual class by status class: `status >= 200 && status < 300 → 'trigger-result-dialog--success'`; `status >= 400 && status < 500 → 'trigger-result-dialog--warn'`; else (`>= 500` or `null`) → `'trigger-result-dialog--error'`. Remove all three classes before adding the chosen one.
- [x] 4.2 In `result-dialog.js`, prepend a `<div class="trigger-result-status">` to the blocks container containing an outcome word (`Success` / `Failed` / `Error`), the HTTP status (or `"network"` for rejection), and `body.error` if it's a string.
- [x] 4.3 Update the `showResult` convenience wrapper and its call sites in `trigger-forms.js` to pass `(response.status, parsedBody)` instead of `(result, ok)`.
- [x] 4.4 In `workflow-engine.css`, add `.trigger-result-dialog--success` (green left border + light-green banner bg) and `.trigger-result-dialog--warn` (amber left border + light-amber banner bg); update `.trigger-result-dialog--error` to match the same pattern with red. Style `.trigger-result-status` (banner typography, flex layout for icon + text). Use the colour-role tokens from task 1.4 for the backgrounds.
- [x] 4.5 Add a visually-hidden `<div role="status" aria-live="polite">` inside each `.trigger-result-copy` button's parent; toggle its text to "Copied" on successful copy and clear after the 2-second timeout.

## 5. Dashboard behaviour

- [x] 5.1 In `packages/runtime/src/ui/dashboard/page.ts`, change `formatTimestamp` to emit an `<time class="entry-started" datetime="<ISO>">{ISO-fallback}</time>` element from `renderCardSummary`; remove the ISO-fallback call (so the initial textContent is the raw ISO string).
- [x] 5.2 Create `packages/runtime/src/ui/static/local-time.js` — on `DOMContentLoaded` and on `htmx:afterSwap`, query `time[datetime]`, set `textContent = new Date(el.getAttribute('datetime')).toLocaleString(undefined, {dateStyle:'medium', timeStyle:'medium'})`. Do NOT mutate the `datetime` attribute. Add `<script defer src="/static/local-time.js">` to the dashboard head block in `dashboard/page.ts`.
- [x] 5.3 In `renderInvocationList`, expose the count as `data-count` on the list root (the outer container that receives the HTMX swap). Above the list, render a header strip containing `N invocations · newest first · updated <time datetime=ISO>{ISO}</time>` (the updated timestamp picks up local-time rewrite automatically). The header is omitted when the list has zero invocations (the existing empty-state message handles that case).
- [x] 5.4 In `renderCardSummary`, split the summary into two rows: row 1 holds `triggerKindIcon` + `.entry-workflow` (bold) + separator (`›` or `·`) + `.entry-trigger` (muted, same colour as `.entry-meta`) + right-aligned chevron (expandable rows only); row 2 holds `.entry-started` + `.entry-sep` + `.entry-duration` + right-aligned `.badge` (moved here from row 1). Add the `expandable: boolean` parameter so pending rows omit the chevron. Update `renderCard` call sites accordingly.
- [x] 5.5 In `workflow-engine.css`, add `.entry-expand-chevron { margin-left: auto; color: var(--text-muted); font-size: var(--fs-md); transition: transform 0.2s ease; display: inline-block; }` and `.entry-expandable[open] > summary .entry-expand-chevron { transform: rotate(90deg); }`; add `.entry-expandable > summary:hover { background: color-mix(in srgb, var(--text) 3%, transparent); }`.
- [x] 5.6 In `workflow-engine.css`, change `--kind-action: #6366f1` to `--kind-action: #14b8a6` (teal); confirm the flamegraph and any dashboard badges that reference `--kind-action` still read against both light and dark card backgrounds.
- [x] 5.7 In `dashboard/page.ts` list rendering, add `aria-busy="true"` to the invocation-list root while skeleton cards are present; include a small inline fragment-level script (or a `htmx:afterSwap` listener inside `local-time.js`) that flips `aria-busy` to `false` after HTMX swaps in the real list.
- [x] 5.8 In `renderCard`, add `aria-label="Expand invocation details"` to the `<summary>` of expandable entries.

## 6. Flamegraph rendering

- [x] 6.1 In `packages/runtime/src/ui/dashboard/flamegraph.ts`, update the marker emitter: each `<rect class="marker-set">`, `<rect class="marker-clear-bg">`, and `<circle class="marker-call">` SHALL have a child `<title>{event.kind}</title>` (escaped via existing `escapeHtml`).
- [x] 6.2 In `workflow-engine.css`, saturate the marker fills and add a 1px white stroke: `.marker-set { fill: var(--kind-rest); stroke: #fff; stroke-width: 1 }`, same treatment for `.marker-clear-bg` and `.marker-call` (use the `--on-accent` token from task 1.4 for the white stroke). Keep sizes unchanged.
- [x] 6.3 In `flamegraph.ts`, define a new constant `ERROR_ICON_RESERVE_PCT` (e.g. `1.5`). In the text-layer loop, when `bar.errored`, compute the duration's x as `x + width - BAR_LABEL_X_INSET_PCT - ERROR_ICON_RESERVE_PCT` so the `⚠` sits to its right at the bar edge. Update the warn-icon x to use the old right-edge math.
- [x] 6.4 In `flamegraph.ts` `renderFlamegraphFragment` (the function that emits the summary + ruler), replace the single-line summary with a compact two-line header: line 1 = bold workflow/trigger + right-aligned success/failed chip (reuse `.badge.succeeded|failed`); line 2 = prominent duration (`--fs-md`) + nonzero counts rendered as muted tokens (e.g. `3A · 2T`); suppress any count of zero entirely.
- [x] 6.5 In `flamegraph.ts`, emit a new `<div class="flame-legend">` element between the header and the ruler containing colour swatches for each kind present in the current layout (`trigger`, `action`, `rest`, `orphan`) and, if any markers are present, two marker examples (× = timer cleared, ● = host call). Only render swatches/examples whose element type is actually present in the fragment.
- [x] 6.6 In `flamegraph.ts` for each orphan bar emit a trailing `⇥` glyph at the right edge (a new `<text class="bar-orphan-glyph">`) and a child `<title>No terminal event recorded</title>` on the bar's `<rect>`.
- [x] 6.7 In `workflow-engine.css`, style `.flame-legend` (flex row, `--sp-3` gap, muted text, `--fs-xs`), `.flame-legend-swatch` (12×12 rounded square coloured by its kind variable), and `.bar-orphan-glyph` (positioned via `dominant-baseline:middle`, uses `--on-accent` for contrast against the orphan-hatched pattern).

## 7. Layout and theme parity

- [x] 7.1 In `packages/runtime/src/ui/layout.ts`, wrap the topbar user section in `<div class="topbar-user" role="group" aria-label="Signed in as <user>">`; ensure the sign-out button sits inside. Re-style `.topbar-signout` in `workflow-engine.css` to drive from `--btn-bg`, `--btn-bg-hover`, `--btn-fg` tokens (rather than hardcoded `#24292f`/`#32383f`/`#fff`); declare the token values in `:root` AND override them in the `@media (prefers-color-scheme: dark)` block so the button reads correctly in both modes.
- [x] 7.2 Apply the same `--btn-bg` / `--btn-bg-hover` / `--btn-fg` treatment to `.btn--primary` (auth page primary button) and any other button that currently pins literal colours.
- [x] 7.3 Audit gate — run `rg -n '#[0-9a-fA-F]{3,6}|rgba?\(' packages/runtime/src/ui/static/workflow-engine.css packages/runtime/src/ui/static/trigger.css | grep -vE ':root|prefers-color-scheme: dark'`; every remaining hit MUST have a justification comment on the same line OR be moved into a token. Resolve every hit before closing this task.
- [ ] 7.4 Manual smoke test in both OS theme modes: toggle the OS theme, walk `/dashboard`, `/trigger`, the auth page (log out and view the sign-in card), open the result dialog in all three states (success via a passing trigger, warn via an intentional validation failure, error via a crashing handler or by disabling the server and clicking Submit), and open a flamegraph. For each: confirm no surface reads as "pasted from the other theme", focus rings appear on tab navigation, and skeleton shimmer is visible.

## 8. Test alignment

- [x] 8.1 Grep for any existing tests that assert the flat `<details>` structure of the trigger page (`rg -n 'details' packages/runtime/src/ui/trigger` and `rg -n 'trigger-details' packages/runtime`); update them to accept the new workflow-grouped layout (or the bare Submit card for empty-schema triggers).
- [x] 8.2 Grep for tests that assert the flamegraph's single-line summary (`rg -n 'flame-summary|summary line' packages/runtime`); update them to permit the two-line header.
- [x] 8.3 Grep for any test referencing the retired `Run now` control, `source.create`, or `PayloadValidationError` language — if found, rewrite against the dialog-based flow and `entry.fire` executor contract.
- [ ] 8.4 Add tests for the three-state dialog class selection in `showResultBlocks` (given status + body, assert the correct class is present). **Skipped** — `result-dialog.js` is a browser IIFE with no module exports; the repo has no jsdom vitest environment configured, so direct unit testing would require new test infrastructure. The status-class selection is exercised end-to-end through the trigger-page submit flow (tests 8.5) and through manual smoke testing (task 7.4).
- [x] 8.5 Add tests for empty-schema trigger rendering (no `.form-container`) and for formless-submit posting `{}`.
- [x] 8.6 Run `pnpm validate` — must pass (lint, format check, type check, test suite).

## 9. Spec + proposal hygiene

- [x] 9.1 Run `pnpm exec openspec validate refresh-trigger-and-dashboard-ui --strict`; fix any delta issues flagged.
- [ ] 9.2 After implementation and review: run `pnpm exec openspec archive refresh-trigger-and-dashboard-ui` to apply the spec deltas to `openspec/specs/trigger-ui/spec.md` and `openspec/specs/dashboard-list-view/spec.md`.
