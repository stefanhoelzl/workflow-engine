{
	"name": "workflows",
	"type": "module",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "WEBHOOK_TOKEN=${WEBHOOK_TOKEN:-build-time-placeholder} pnpm exec wfe build"
	},
	"dependencies": {
		"@workflow-engine/sdk": "workspace:*"
	}
}
